# src/persistence

Capa de persistencia del `ProjectDocument` (carga/guardado vía comandos
Tauri, autoguardado). Se implementará en una fase posterior; este milestone
solo define el modelo de dominio en memoria.
